$kind: http-request
name: "[FAIL 400] POST /partidos/:id/resultado — puntosLocal no entero"
url: "{{baseUrl}}/partidos/{{idPartido}}/resultado"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: text
  content: '{"puntosLocal":1.5,"puntosVisitante":40}'
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 7000
