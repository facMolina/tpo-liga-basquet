$kind: http-request
name: "[FAIL 400] POST /auth/login — usuario vacío"
url: "{{baseUrl}}/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: '{"usuario":"","password":"adminpassword"}'
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 4000
