$kind: http-request
name: "[FAIL 401] GET /auth/me — header malformado (Token abc)"
url: "{{baseUrl}}/auth/me"
method: GET
headers:
  Authorization: Token abc123
scripts:
  - type: afterResponse
    code: pm.test('status 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 8000
