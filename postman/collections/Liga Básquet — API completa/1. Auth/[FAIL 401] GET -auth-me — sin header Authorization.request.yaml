$kind: http-request
name: "[FAIL 401] GET /auth/me — sin header Authorization"
url: "{{baseUrl}}/auth/me"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 7000
