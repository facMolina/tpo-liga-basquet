$kind: http-request
name: "[FAIL 404] PUT /jugadores/99999 — no existe"
url: "{{baseUrl}}/jugadores/99999"
method: PUT
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: text
  content: '{"nombre":"X"}'
scripts:
  - type: afterResponse
    code: pm.test('status 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 13000
