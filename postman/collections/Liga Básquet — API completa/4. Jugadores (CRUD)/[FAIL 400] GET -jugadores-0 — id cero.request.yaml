$kind: http-request
name: "[FAIL 400] GET /jugadores/0 — id cero"
url: "{{baseUrl}}/jugadores/0"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 14000
