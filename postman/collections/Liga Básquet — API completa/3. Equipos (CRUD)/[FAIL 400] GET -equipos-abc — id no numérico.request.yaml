$kind: http-request
name: "[FAIL 400] GET /equipos/abc — id no numérico"
url: "{{baseUrl}}/equipos/abc"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 16000
