$kind: http-request
name: "[FAIL 401] POST /equipos — sin token"
url: "{{baseUrl}}/equipos"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: "{\r

    \    \"nombre\": \"X\",\r

    \    \"entrenador\": \"Y\"\r

    }"
scripts:
  - type: afterResponse
    code: pm.test('status 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 7000
