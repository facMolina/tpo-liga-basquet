$kind: http-request
name: "[FAIL 404] PUT /equipos/99999 — no existe"
url: "{{baseUrl}}/equipos/99999"
method: PUT
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: text
  content: '{"entrenador":"X"}'
scripts:
  - type: afterResponse
    code: pm.test('status 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 15000
