$kind: http-request
name: "[FAIL 400] GET /partidos?estado=invalido — query inválida"
url: "{{baseUrl}}/partidos?estado=invalido"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 14500
