$kind: http-request
name: "[OK] GET /partidos?desde=...&hasta=... — filtro calendario por rango"
url: "{{baseUrl}}/partidos?desde=2026-01-01&hasta=2026-12-31"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('status 200', () => pm.response.to.have.status(200));
      pm.test('response es array', () => pm.expect(pm.response.json()).to.be.an('array'));
    language: text/javascript
order: 4700
