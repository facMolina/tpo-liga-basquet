$kind: http-request
name: "[FAIL 400] GET /partidos?desde=mal — fecha mal formateada"
url: "{{baseUrl}}/partidos?desde=01-01-2026"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 14600
