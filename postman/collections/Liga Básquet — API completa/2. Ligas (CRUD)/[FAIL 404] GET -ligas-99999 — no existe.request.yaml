$kind: http-request
name: "[FAIL 404] GET /ligas/99999 — no existe"
url: "{{baseUrl}}/ligas/99999"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 9000
