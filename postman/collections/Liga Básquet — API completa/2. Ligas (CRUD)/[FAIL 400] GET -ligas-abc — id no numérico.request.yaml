$kind: http-request
name: "[FAIL 400] GET /ligas/abc — id no numérico"
url: "{{baseUrl}}/ligas/abc"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 7000
