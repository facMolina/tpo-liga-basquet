$kind: http-request
name: "[OK] GET /ligas — listar"
url: "{{baseUrl}}/ligas"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('status 200', () => pm.response.to.have.status(200));
      pm.test('response es array', () => pm.expect(pm.response.json()).to.be.an('array'));
    language: text/javascript
order: 2000
